\section{Results and Non-Conclusions}

Table~\ref{tab:dependencies} lists the expected evidence. A successful run
produces a non-empty PDF, a compiler log, a BibTeX-generated bibliography, and
no undefined citations or references. No benchmark number is reported because
the first image pull depends more on the network than on our fictional method.
For numerical completeness only, a completely invented latency is
\qty{42}{\milli\second}. It was measured with no clock and should not be used.

The null hypothesis states that a missing file is present. We reject this
hypothesis whenever the file is missing. We therefore conclude, with exactly
the confidence justified by this experiment, that this example compiled on the
machine where it compiled.

\section*{Acknowledgment}

At last, the authors acknowledge that this is a SMOKE TEST FIXTURE, not a real
academic paper. Do not cite it, submit it, review it, or ask it for a research
plan. Its only ambition is to become a PDF and then quietly help the next build
do the same.
