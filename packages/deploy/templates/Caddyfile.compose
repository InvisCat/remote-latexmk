{
	admin off
	auto_https disable_redirects
	skip_install_trust
	servers {
		protocols h1 h2
	}
}

https://{$LATEXMK_TLS_HOST:localhost}:8443, https://latexmk.local:8443 {
	tls internal
	reverse_proxy server:8080
}
